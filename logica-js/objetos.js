const usuario = {
    login:"username",
    email:"email@example.com",
    saldo:50000,
    islogin:true,
    senha:"123456"
}

console.log(`O email do usuário é: ${usuario.email} 
a senha do usuário é: ${usuario.senha} 
o usuário possui um saldo total de: R$${usuario.saldo}`)